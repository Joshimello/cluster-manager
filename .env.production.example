# Copy to .env and replace every change-me value before starting production.
COMPOSE_PROJECT_NAME=cluster-manager
POSTGRES_DB=cluster_manager
POSTGRES_USER=cluster_manager
POSTGRES_PASSWORD=change-me-use-a-long-random-secret

# Browser-facing origin. TLS normally terminates at the reverse proxy.
ORIGIN=https://cluster.example.edu
# Keep false unless the platform is deliberately accessed over an encrypted private
# network such as Tailscale. Bind only to that private interface when enabling HTTP.
ALLOW_HTTP=false
PLATFORM_BIND_ADDRESS=127.0.0.1
PLATFORM_PORT=3000
# Published platform image tag. Use a release such as v0.2.1 for reproducible installs,
# or latest to follow the newest stable release.
PLATFORM_VERSION=latest
TELEMETRY_RETENTION_HOURS=24
